#include <iostream>
#include <math.h>

bool is_square(int num);

int main() {
    int num = 47;
    bool x = is_square(num);
    std::cout<< x<< std::endl;
}

bool is_square (int num) {
    
}